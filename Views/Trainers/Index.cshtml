@model IEnumerable<FitnessCenterManagement.Models.Trainer>

@{
    ViewData["Title"] = "Trainers";
}

<div class="toolbar">
    <div>
        <h1 class="page-title mb-1">Trainers</h1>
        <div class="muted">Eğitmen listesini görüntüleyebilirsin.</div>
    </div>

    @if (User.IsInRole("Admin"))
    {
        <a class="btn btn-primary" asp-action="Create">+ Create Trainer</a>
    }
</div>

<div class="card">
    <div class="card-header d-flex align-items-center justify-content-between">
        <strong>Trainer Listesi</strong>
        <span class="muted">@Model.Count() kayıt</span>
    </div>

    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover mb-0 align-middle">
                <thead>
                    <tr>
                        <th>Full Name</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="fw-semibold">@item.FullName</td>
                            <td class="text-end">
                                <a class="btn btn-sm btn-outline-secondary" asp-action="Details" asp-route-id="@item.Id">Details</a>

                                @if (User.IsInRole("Admin"))
                                {
                                    <a class="btn btn-sm btn-outline-primary" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                                    <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                                }
                            </td>
                        </tr>
                    }

                    @if (!Model.Any())
                    {
                        <tr>
                            <td colspan="2" class="text-center p-4 muted">
                                Henüz trainer yok.
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
